# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts

# env — never commit secrets; .env.example is the only tracked env file
.env
.env.*
!.env.example

# builds & caches
*.tsbuildinfo
coverage/
playwright-report/
test-results/

# vercel
.vercel/

# drizzle local artifacts (migrations themselves ARE committed)
drizzle/.tmp/

# local embedded Postgres (dev fallback)
.pglite/

# os / editor
.DS_Store
Thumbs.db

.vercel
.env*
