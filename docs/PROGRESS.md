# Progress

Milestone checklist — tick items as they are finished and verified (typecheck + lint + build green, app clicked through, committed).

- [x] **0 — Foundation.** Repo, `docs/SPEC.md`, `CLAUDE.md`, `PROGRESS.md`, `DECISIONS.md`, `.env.example`, `.gitignore`, npm scripts (scripts land with the scaffold in M1).
- [x] **1 — Shell & design system.** Next 16.2.10 + TS strict + Tailwind 4 + shadcn (radix, RTL init). All 12 theme variants (6 palettes × light/dark) pass `check-contrast` at strict thresholds (text 4.5:1, UI 3:1, CVD ΔE ≥ 0.08, normal-vision ≥ 0.15), gate wired into `npm run build`. Shell: icon-collapsible sidebar, topbar with breadcrumbs, Ctrl+K palette, `g`-chord nav, `/` and `?` shortcuts, theme picker, density toggle. Overview wired to fake data (KPIs, revenue/costs area chart, category donut, top products, transactions, alerts). Verified in headless Edge across 4 themes + mobile viewport, zero console errors.
- [x] **2 — Database.** Full §6 Drizzle schema (money `numeric(14,4)`, soft deletes, idempotency index on import_rows), migrations in git, driver-agnostic client (Neon HTTP when `DATABASE_URL` set, embedded PGlite fallback for local dev). Seed: 44 products, 30 parties, 1,422 transactions across 18 months with seasonal shape (Eid spikes, spring lambing, honey harvest, winter milk dip) and 5 deliberate anomalies; herd ledger guarantees §9 reconciliation balances (verified by `scripts/verify-seed.ts`). `db:generate/migrate/seed/studio` scripts all work.
- [x] **3 — Auth.** Better Auth (username + admin plugins, drizzle adapter), designed split-screen login page, `requireUser()` first line of every data page (headers-first so builds stay DB-free), middleware as UX-redirect-only, DB-backed rate limiting (verified: 5 wrong attempts → 429, correct password also 429 inside window), admin seeded from env on first run, change-password + viewer accounts in /settings, security headers + CSP + robots Disallow. Verified over HTTP and headless Edge: redirects, generic 401, login → populated dashboard, admin-only Users section.
- [x] **4 — Products CRUD.** TanStack v8 table (sort, fuzzy search, category/status filters, column visibility, selection, pagination, virtualization >500 rows, saved views in localStorage, density-aware). Row actions: edit in slide-over sheet, duplicate, adjust stock (reason → adjustment ledger row), archive, soft delete with 5s undo toast. Bulk: price change (%/absolute), category reassign, archive, Excel export (SheetJS 0.20.3 pinned from cdn.sheetjs.com). Form: creatable category/unit comboboxes, dynamic custom fields per category + "Add custom field" that persists an attribute_def, live Decimal calc strip (margin/markup/stock value). Detail page: KPIs, sales chart, attributes, ledger, audit history. All mutations admin-only, zod-validated server-side, audit-logged. Verified end-to-end in headless Edge: create with new category + new unit + new custom field, delete → undo restore, no console errors.
- [x] **5 — Import engine.** Full §5 pipeline: Web-Worker parsing (header detection under merged title rows, merge forward-fill, trailing blank trim), per-column type inference with Excel number-format evidence, EN+AR synonym dictionary + fuzzy role matching (exact-first to avoid short-word collisions), ambiguous-date flagging with DMY/MDY confirmation, qty×price≈total cross-check with >1% anomaly rows, mixed-unit detection, currency detection. Review UI: editable mapping, clickable quality panel, live normalized preview, save/auto-apply mapping profiles keyed on sha256 header signature. Chunked idempotent upload (unique row index + ON CONFLICT), commit creates transactions/products/parties with full provenance, one-click rollback removes everything (verified in DB: 0 rows left, created products archived). 37 Vitest unit tests green. End-to-end driven in headless Edge with a deliberately messy workbook.
- [ ] **6 — Auto dashboard.** Widget generator, chart library, layout persistence, drill-down to rows, data explorer.
- [ ] **7 — Analytics.** §9 — reconciliation, margins, yields, deltas, pivot builder, exports.
- [ ] **8 — Polish.** Every empty/loading/error state. A11y pass. Mobile pass. RTL check. Lighthouse ≥90. Playwright happy path.
- [ ] **9 — Ship.** Env vars, migrate, seed, `vercel --prod`, verify live, README.
